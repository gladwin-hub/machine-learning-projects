# -*- coding: utf-8 -*-
"""prediction_of_diabetic_patient.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1M6dUDMfCi7_PiOnyzF2G0USqBpdz9m1j
"""

import numpy as np
import pandas as pd
from sklearn.preprocessing import StandardScaler
from sklearn.model_selection import train_test_split
from sklearn import svm
from sklearn.metrics import accuracy_score

df=pd.read_csv('/content/diabetes.csv')

df.head()

x=df.drop(columns='Outcome',axis=1)
y=df['Outcome']

print(x)
print(y)

scaler=StandardScaler()

scaler.fit(x)
std1=scaler.transform(x)

print(std1)

x=std1
y=df['Outcome']

x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,stratify=y,random_state=2)

model=svm.SVC(kernel='linear')

model.fit(x_train,y_train)

pred_train=model.predict(x_train)
accuracy=accuracy_score(pred_train,y_train)
print(accuracy)

pred_test=model.predict(x_test)
accuracy1=accuracy_score(pred_test,y_test)
print(accuracy1)

input_data=(1, 85, 66, 29, 0, 26.6, 0.351, 31)
numpy=np.asarray(input_data)
reshape=numpy.reshape(1,-1)
std=scaler.transform(reshape)
pred=model.predict(std)
print(pred)
if(pred[0]==0):
  print("patient is diabetic")
else:
  print("patient is not diabetic")

